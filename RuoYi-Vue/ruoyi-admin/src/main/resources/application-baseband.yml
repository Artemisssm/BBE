# 基带监控模块配置
baseband:
  # 组播配置
  multicast:
    # 组播地址
    address: 239.255.0.1
    # 组播端口
    port: 9000
    # TTL值（跳数限制）
    ttl: 16
    # 网络接口名称（可选，如：eth0, en0）
    # 如果不指定，系统会使用默认网络接口
    # netIf: eth0
